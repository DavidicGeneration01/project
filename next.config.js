/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactStrictMode: true,       // example option
  swcMinify: true,             // example option for faster builds
  // Add any other Next.js config options you had in your TS file
};

module.exports = nextConfig;
